import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    videoContainer: {
        width: '100%',
        aspectRatio: 16 / 9,
        marginTop: 20,
        alignItems: 'center',
    },
});
export default styles;
