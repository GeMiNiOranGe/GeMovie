import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    videoContainer: {
        width: '100%',
        aspectRatio: 16 / 9,
    },
    text: {
        color: 'white',
        fontSize: 16,
        textAlign: 'center',
    },
});
export default styles;
