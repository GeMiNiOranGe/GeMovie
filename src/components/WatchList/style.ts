import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        borderRadius: 50,
        padding: 5,
        zIndex: 10,
        left: -7,
        top: -5,
    },
    bookmarkIcon: {
        position: 'absolute',
    },
    addIcon: {
        position: 'absolute',
        top: 6,
        left: 10,
    },
});
export default styles;
