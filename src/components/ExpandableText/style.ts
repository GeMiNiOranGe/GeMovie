import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    text: {
        fontSize: 16,
        lineHeight: 24,
        color: 'black',
    },
    toggleText: {
        fontSize: 14,
        color: '#007BFF',
        marginTop: 5,
    },
});

export default styles;
