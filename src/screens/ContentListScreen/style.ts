import { StyleSheet } from 'react-native';

import { colors, fontSizes } from '@shared/themes';

const styles = StyleSheet.create({
    text: {
        fontSize: fontSizes.h4,
        color: colors.text,
    },
});

export default styles;
